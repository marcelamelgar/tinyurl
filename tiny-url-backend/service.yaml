apiVersion: v1
kind: Service
metadata:
  name: tinyurl-backend-service
spec:
  type: LoadBalancer
  selector:
    app: tinyurl-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000
